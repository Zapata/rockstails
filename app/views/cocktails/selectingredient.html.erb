<h1>Select one ingredient to add</h1>

<% form_tag(addingredient_cocktail_path, :method=>'put') do %>
  <% select :p, :ingredient, @ingredients %>
 <%= select_tag("Ingrédient", options_for_select(@ingredients)) %>

<% end %>

<%= link_to 'Back', @cocktail %>
